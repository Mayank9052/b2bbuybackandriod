name: b2b_buyback
description: "B2B buyback Android starter application."
publish_to: 'none'
version: 1.0.0+1

# environment:
#   sdk: ^3.11.5
environment:
  sdk: ">=3.11.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # HTTP
  http: ^1.6.0

  # Local storage for JWT / dealer session
  shared_preferences: ^2.5.5

  # Icons
  cupertino_icons: ^1.0.8

  # Optional: for future SMS auto-read
  # sms_autofill: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # If you add a logo asset:
  # assets:
  #   - assets/images/logo.png